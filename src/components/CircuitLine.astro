<div class="circuit-line" aria-hidden="true">
  <svg viewBox="0 0 20 1000" preserveAspectRatio="none">
    <path id="circuitPath" d="M10,0 L10,1000" pathLength="1000" />
  </svg>
  <div class="circuit-plug" id="circuitPlug">
    <i class="fas fa-plug"></i>
  </div>
</div>

<style>
  .circuit-plug {
    position: absolute;
    left: 50%;
    bottom: 6vh;
    transform: translateX(-50%) rotate(90deg);
    width: 26px;
    height: 26px;
    border-radius: 50%;
    background: var(--graphite);
    border: 1px solid var(--slate-line);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--muted-on-dark);
    font-size: 0.7rem;
    transition: color 0.4s ease, border-color 0.4s ease, box-shadow 0.4s ease;
  }

  .circuit-plug.is-live {
    color: var(--signal);
    border-color: var(--signal);
    box-shadow: 0 0 14px 1px var(--signal-line);
  }
</style>
